meta {
  name: Update / Self
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/users/me
  body: json
  auth: inherit
}

body:json {
  {
    "birthDate": "1996-12-19"
  }
}

settings {
  encodeUrl: true
}

docs {
  ### Body DTO
  ```json
  {
    email?: string;
    username?: string;
    password?: string;
    birthDate?: Date;
    roles?: Role[];
  }
  ```
}
