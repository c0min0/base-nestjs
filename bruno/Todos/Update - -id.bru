meta {
  name: Update / :id
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/todos/:id
  body: json
  auth: inherit
}

params:path {
  id: 
}

body:json {
  {
    "description": "This is a change in the todo 01 description"
  }
}

settings {
  encodeUrl: true
}

docs {
  ### Body DTO
  ```json
  {
    title?: string;
    description?: string;
  }
  ```
}
